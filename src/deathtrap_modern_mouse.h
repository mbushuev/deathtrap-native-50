#pragma once

// Adds the mouse sources already implemented by Dungeon.dll to the native
// gameplay action map. The menu keeps using the game's original mouse path.
void InitializeDeathtrapModernMouse();
bool InstallDeathtrapModernMouseHook();

